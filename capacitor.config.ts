import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.comerciantes.comerciantesrestrepo',
  appName: 'Comerciantes_Restrepo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
